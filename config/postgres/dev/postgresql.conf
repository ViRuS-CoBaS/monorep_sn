#------------------------------------------------------------------------------
# CONNECTION SETTINGS
#------------------------------------------------------------------------------
max_connections = 100
# Достаточно для локальной разработки

#------------------------------------------------------------------------------
# RESOURCES (Память и Кеширование)
#------------------------------------------------------------------------------
# Предполагаем, что у вас 8GB+ RAM на машине разработки
shared_buffers = 1GB
# Достаточно для кэширования в Dev-режиме
effective_cache_size = 4GB
# Указываем планировщику, что много памяти доступно
maintenance_work_mem = 128MB
# Ускоряет VACCUM и CREATE INDEX в Dev-среде
work_mem = 32MB
# Достаточно памяти для JOIN и сортировок в простых Dev-запросах

#------------------------------------------------------------------------------
# WAL & CHECKPOINTS (ОПТИМИЗАЦИЯ СКОРОСТИ ЗАПИСИ)
#------------------------------------------------------------------------------
# 💡 КРИТИЧЕСКИЕ НАСТРОЙКИ ДЛЯ СКОРОСТИ В DEV-РЕЖИМЕ
fsync = off
# ОТКЛЮЧЕНО! Запрещает принудительную запись на диск. Дает БОЛЬШОЙ прирост скорости.
# ⚠️ ПОТЕРЯ ДАННЫХ ПРИ СБОЕ ГАРАНТИРОВАНА.
synchronous_commit = off
# ОТКЛЮЧЕНО! Транзакции подтверждаются немедленно, не дожидаясь записи в WAL.
# Дает БОЛЬШОЙ прирост скорости. ⚠️ ПОТЕРЯ ДАННЫХ ПРИ СБОЕ ГАРАНТИРОВАНА.

wal_level = minimal
# Минимальный уровень логирования WAL. Ускоряет запись.
max_wal_size = 1GB
# Уменьшен, так как объем данных в Dev-режиме мал.
min_wal_size = 80MB

#------------------------------------------------------------------------------
# QUERY TUNING (Оптимизация чтения)
#------------------------------------------------------------------------------
random_page_cost = 1.5
# Предпочтение индексам (для SSD)

#------------------------------------------------------------------------------
# ERROR REPORTING AND LOGGING (Логи)
#------------------------------------------------------------------------------
log_min_duration_statement = -1
# ОТКЛЮЧЕНО! Не логируем медленные запросы, чтобы не засорять логи разработки.
logging_collector = off
# Отключение коллектора логов (если вы не используете его активно).